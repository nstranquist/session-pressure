---
{
  "generated_at": "2026-05-13T22:19:45.175928Z",
  "import_path": "nicos-dev/internal/lockfile",
  "package": "lockfile",
  "schema": 1,
  "stats_exported": 3,
  "stats_files": 1,
  "stats_loc": 210,
  "surface_hash": "36de197467000a62"
}
---

# `nicos-dev/internal/lockfile` — package runbook

_3 exported symbols across 1 files (210 LOC). Surface hash `36de197467000a62`._

## Public functions

### `Lock`

```go
func Lock(path string) (func(), error)
```

> Lock acquires an advisory lock for path and returns an idempotent unlock
> function. The lock state is stored in path+".lock".

_Source: `nicos-dev/internal/lockfile/lockfile.go:37`_

### `WithFileLock`

```go
func WithFileLock(path string, fn func() error) error
```

> WithFileLock runs fn while holding Lock(path).

_Source: `nicos-dev/internal/lockfile/lockfile.go:91`_

## Vars

### `ErrLocked`

```go
var ErrLocked = errors.New("lockfile: lock held by another process")
```

> ErrLocked is returned when a lock cannot be acquired before the timeout.

_Source: `nicos-dev/internal/lockfile/lockfile.go:25`_

## Error variables

- `ErrLocked` (nicos-dev/internal/lockfile/lockfile.go:25) — ErrLocked is returned when a lock cannot be acquired before the timeout.

## Callers (top references)

- `nicos-dev/ai/providers/usage.go` — 1 ref(s), first symbol `WithFileLock`
- `nicos-dev/cmd/claude-hook/main.go` — 1 ref(s), first symbol `WithFileLock`
- `nicos-dev/cmd/codex-hook/main.go` — 1 ref(s), first symbol `WithFileLock`
- `nicos-dev/cmd/kimi-hook/main.go` — 1 ref(s), first symbol `WithFileLock`
- `nicos-dev/internal/ai/mode/apply.go` — 1 ref(s), first symbol `Lock`
- `nicos-dev/internal/ai/mode/history.go` — 1 ref(s), first symbol `WithFileLock`
- `nicos-dev/internal/audit/audit.go` — 1 ref(s), first symbol `WithFileLock`
- `nicos-dev/internal/claudemonitor/store.go` — 1 ref(s), first symbol `WithFileLock`

## Tribal knowledge

<!-- HUMAN-SLAB:START -->
_Empty slab. Fill with: invariants the source can't carry, surprising behaviors, cross-package coupling that isn't captured by imports, incident postmortems._
<!-- HUMAN-SLAB:END -->

## Source files

- `nicos-dev/internal/lockfile/lockfile.go`

---
_Generated by `ndev kb runbook generate`. The HUMAN slab is preserved across regeneration; everything else is overwritten._
